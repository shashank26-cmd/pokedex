import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'
import { BrowserRouter } from 'react-router-dom'
ReactDOM.createRoot(document.getElementById('root')).render(

<BrowserRouter>
<App />
</BrowserRouter>

// 1- app component ko browser router se cover krdiya hai so react router dom enable harr jagah
// 2- custom routes
//3-app.jsx render custom routes and u can add some features here
//4-pokemon mai jake name and img target if click so pass at pokemon.id
//5-now add logic ki if click so parms se id trget and us pokem ke utl msi jake sab copy karke now set krke return now it will show ans
//6--donee till part 3 now part 4
)
